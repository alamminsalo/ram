Dog:
  x-sql-table: "dog"
  x-sql-pk: "id"
  properties:
    id:
      type: integer
    name:
      type: string
    type:
      type: string

Cat:
  x-sql-table: "cat"
  x-sql-pk: "id"
  properties:
    id:
      type: integer
    name:
      type: string
    type:
      type: string

Cow:
  x-sql-table: "cow"
  x-sql-pk: "id"
  properties:
    id:
      type: integer
    name:
      type: string
    type:
      type: string
